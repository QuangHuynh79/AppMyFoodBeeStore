dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
        jcenter()
        maven { url"https://maven.xyz.com"}
        // Warning: this repository is going to shut down soon
    }
}
rootProject.name = "My Food Bee Store"
include ':app'
